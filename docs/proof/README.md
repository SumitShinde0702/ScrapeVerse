# Proof artifacts

- `live-collect/` — outputs from `npm run prove:live` (Proof A)
- `heal/` — outputs from `npm run prove:heal` (Proof B)

Commit redacted samples after a live Bright Data run. Mock runs are fine for CI/dev.
